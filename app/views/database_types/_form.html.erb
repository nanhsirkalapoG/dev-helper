<%= form_with(model: database_type, local: true) do |form| %>
  <% if database_type.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(database_type.errors.count, "error") %> prohibited this database_type from being saved:</h2>

      <ul>
      <% database_type.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="field">
    <%= form.label :notes %>
    <%= form.text_field :notes %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
