<%= form_with(model: ide, local: true) do |form| %>
  <% if ide.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(ide.errors.count, "error") %> prohibited this ide from being saved:</h2>

      <ul>
      <% ide.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="field">
    <%= form.label :notes %>
    <%= form.text_field :notes %>
  </div>

  <div class="field">
    <%= form.label :open_source %>
    <%= form.check_box :open_source %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
